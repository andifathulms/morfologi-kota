node_modules/
.next/
/out/
.DS_Store
*.tsbuildinfo
next-env.d.ts
# Raw extracts are never committed; the derived bundles in data/out are.
data/cache/
# Generated by scripts/publish-data.mjs from data/out, which is committed.
public/data/
coverage/
# Brand masters. The exported PNG and SVG originals are working files, not
# product: what the app and the README actually serve is committed under
# `app/`, `public/brand/` and `.github/brand/`, so the build never depends on
# a folder that is not in the repository.
exports/
